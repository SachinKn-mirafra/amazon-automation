exports.CheckoutPage = class CheckoutPage {

    constructor(page, expect) {
        this.page = page;
        this.expect = expect;
    }

    async verifyTotalPrice(packageObject) {
        // to verify this we need to login so im stoping this here.
    }

}